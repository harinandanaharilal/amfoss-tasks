# Crypto Tracker
<p> First using html we set the language to english and theme as dark themed.The <head> to include the
main document part of the html which includes <meta> ,<title>,<script> etc.The character set UTF-8 was 
for using a wide variety of symbols and other texts.Then we set it to fit any device ,Then the title is given
.Then the different stylesheet is imported ,i have used "https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" 
.Then the scripting part which is for the charting of the graph in the crypto tracker.After that <style> is used
for giving background,font colours and other styling which is visible on screen. Then the main body is added 
which includes navigation header,control bar and other necessary things.Again a script is used where
the dataset which was linked in the task was used ,This becomes the dat which we see on the website.
After that differnet functions were used like for updating the char,getting the elements by search etc
In this CSS part is the styling part and The scripting part was the javacript part.
